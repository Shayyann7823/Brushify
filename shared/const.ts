// Minimal stand-in for the original (missing) shared/const.ts.
// Only the values actually consumed by the client at runtime are defined here.

export const COOKIE_NAME = "manus-session";
export const ONE_YEAR_MS = 365 * 24 * 60 * 60 * 1000;
export const UNAUTHED_ERR_MSG = "UNAUTHENTICATED";
